interface Grocery {
    item: string;
    quantity: number;
  }
  
  class GroceryList {
    item: string;
    quantity: number;
  
    constructor(item: string, qunatity: number) {
      this.item = item;
      this.quantity = this.quantity;
    }
  }
  
type ItemArray = Array<string>;
const item = ['milk', 'bread', 'eggs', 'apples', 'carrots'];

type QuantityArray = Array<number>;
const quantity = [3, 1, 6, 5, 8];
